- paths = paths || {}
- paths.root = _root
- modifyPath = function(path) {return path ? path.replace(config.build, paths.root) : ''; }
- paths.static = paths.root + 'assets/'
- paths.img = modifyPath(paths.dest.img) || paths.static + 'img/' 
- paths.js = modifyPath(paths.dest.js) || paths.static + 'js/' 
- paths.css = modifyPath(paths.dest.css) || paths.static + 'css/' 